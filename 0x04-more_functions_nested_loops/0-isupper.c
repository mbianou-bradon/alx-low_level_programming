#include "main.h"

/**
 * _isupper : Checks for uppercase
 * @d: Character to check for case
 *
 * Return: 1 if uppercase, 0 if not uppercase
 */

int _isupper(int d)
{
	if(d >= 'A' && d <= 'Z'){
		return (1);
	}
	return (0)
}
